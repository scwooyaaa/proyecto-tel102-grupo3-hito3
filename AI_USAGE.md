# IA utilizado
Gemini 3 Pro

# Prompts
- Fue entregado el archivo pdf con las instrucciones del hito 3, el código de hito anterior y el siguiente prompt: implementa la parte 1 utilizando el siguiente código en c++.

# Observaciones
Se utilizó el IA para generar un código base del cual partir y realizar el proyecto utilizando los elementos base entregados y así transformar el código que ya se tenía desde el hito anterior a uno con los signals, slots y clases propias de un programa en qt. Se tuvo cuidado con cambiar lo necesario para que fuera compatible y se creó el programa desde el mismo qt con las clases distribuidas por este mismo. Todo esto siempre teniendo en cuenta que las IAs son programas autogenerados y que pueden presentar errores en su ejecución.
